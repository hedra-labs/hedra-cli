# Hand-owned Fern Replay config (.fernignore'd). Generation only ever reads it —
# the one code path that writes it is `fern replay bootstrap` with .fernignore
# migration, which the generate path never asks for. Companion to .fernignore,
# but the opposite lever:
#
#   .fernignore  — "generator, do not WRITE this file"        (freezes the file)
#   exclude here — "replay, do not RE-APPLY customizations"   (frees the file)
#
# A third lever lives in the lockfile rather than here, and the two are not
# interchangeable — see the version-stamp entry below:
#
#   replay forget — "drop this patch from the ledger for good" (retires history)
#
# Replay records every non-.fernignore'd commit since the last generation as a
# "customization" and replays it onto each new generation. Entries here tell it
# not to, so the listed files always land as pure generated output.
#
# Verified semantics, fern-api 5.95.1 — the version generate-sdks.yml actually
# ran on 2026-08-13 ("Installed Fern CLI version 5.95.1"), re-read from the
# published bundle at that exact version. It installs via fern-api/setup-fern-cli
# with version: latest and pins nothing, so this floats; re-read the applicator
# rather than trusting this note after a CLI bump. Re-read at 5.100.0 (`latest`
# as of 2026-08-20): isExcluded, resolveFilePath and the merge planner are
# byte-identical to 5.95.1 modulo minified module aliases.
#
#   - `exclude` suppresses patch APPLICATION, not detection.
#     ReplayApplicator.applyPatches() tests isExcluded(patch) at the top of the
#     loop and pushes {status:"skipped"} before any apply attempt, so an
#     excluded patch can never conflict or go "unresolved" ("Action required"
#     on a regeneration PR). Detection still records it in replay.lock's
#     `patches:` ledger — the entry just stays inert there.
#   - Entries are minimatch globs against repo-relative paths, read from THIS
#     file: LockManager.getCustomizationsConfig() YAML-parses
#     <outputDir>/.fern/replay.yml on every call and returns {} if it is absent.
#   - Detection is `git log --no-merges <last-generation>..HEAD -- <outputDir>`,
#     filtered only by author and subject: fern-api[bot]'s own commits, and
#     messages starting `[fern-generated]` / `[fern-replay]` / `[fern-autoversion]`
#     (plus a few "Generated by Fern" variants), are skipped. Nothing else is.
#     A commit authored by hedra-sdk-ci-app[bot] — refresh-lockfile.yml's fixup
#     commit — or by a human patching a regeneration branch by hand IS recorded
#     as a customization. That is new in practice: autoland now lands
#     regeneration PRs with a merge commit rather than a squash (#75), so those
#     commits reach the default branch under their own SHAs instead of being
#     folded into one fern-api[bot] commit the filter would have skipped.
#
# WHOLE-PATCH SEMANTICS: isExcluded() is `patch.files.some(f => ...)` — a patch
# is skipped ENTIRELY if ANY of its files matches. Keep version bumps in
# dedicated commits, or a mixed commit will drag real customizations into the
# skip along with the bump.
#
# That hazard used to block the src/sdk_executor.rs entry below, and no longer
# does: patch-d06b3c36 was recorded against two files, but the generator stopped
# emitting one of them, so only the half we want skipped is left. The entry
# carries the details.

exclude:
  # Machine-owned version stamps (ENG-10241).
  #
  # Every regeneration resets these two member manifests to the generator's own
  # defaults — hedra_sdk to 0.1.0, hedra_types to 0.0.0 — while the root crate
  # keeps the number `fern generate --version` stamped, because fern-config's
  # `packageIdentity` upserts into the ROOT [package] table and version is not
  # one of its keys. .github/scripts/set-version.py re-aligns the members from
  # the root afterwards; refresh-lockfile.yml carries the full rationale.
  #
  # That alignment commit is what Replay picked up as a customization —
  # patch-ed9886b4, from "ci: align the workspace crate versions on every
  # regeneration (#52)", whose entire diff is:
  #
  #     hedra-sdk/Cargo.toml     -version = "0.1.0"   +version = "1.0.0-dev"
  #     hedra-types/Cargo.toml   -version = "0.0.0"   +version = "1.0.0-dev"
  #
  # Replaying a version stamp is wrong in both directions:
  #
  #   - It re-stamps a stale number. refresh-lockfile.yml masks this today, but
  #     it only fires on `push: fern-bot/**`. release-sdks.yml (fern-config#44)
  #     regenerates and commits straight to main with no bot branch, so on a
  #     release regeneration the replayed "1.0.0-dev" is the last write — the
  #     `-dev` suffix the release exists to strip.
  #   - It will eventually conflict instead of applying. The patch context is
  #     the generator's own `version = "0.1.0"` / `"0.0.0"` lines; the day the
  #     generator emits anything else, the patch goes unresolved and the
  #     regeneration PR shows "Action required". hedra-python hit exactly this
  #     on pyproject.toml when a test regeneration stamped 1.1.0 (its PR #44).
  #
  # Do NOT "fix" this by .fernignore'ing the two manifests instead. That was
  # tried and rejected (ENG-10112 §2, PR #21, never merged): freezing them also
  # freezes real generated dependency changes, the ENG-10048 trap the Cargo.lock
  # entry in .fernignore documents. Excluding here leaves both files fully
  # generator-owned and only stops Replay putting yesterday's stamp back on top.
  #
  # THE ENTRIES ARE FORWARD-LOOKING ONLY. The historical stamp patches are no
  # longer in the ledger at all: `fern replay forget` retired patch-ed9886b4
  # (#52, the pre-rename 1.0.0-dev stamp), patch-6c535920 and patch-c5aa650c
  # (the hedra-sdk-ci-app[bot] alignment commits, stamping a 2.0.2-dev that the
  # tree had already moved past). Forget is terminal in a way exclude is not:
  # it records a `dismissals` entry and pushes each patch's content hash into
  # `forgotten_hashes`, and detection skips a hash listed there, so those
  # commits can never be re-recorded.
  #
  # What remains is the recurring case. refresh-lockfile.yml runs
  # set-version.py after every regeneration, and that alignment commit is
  # authored by hedra-sdk-ci-app[bot] — not fern-api[bot] — with a plain
  # subject, so nothing in the detection filter skips it. The NEXT one is a
  # fresh commit with a fresh content hash that no dismissal covers, and it
  # would be replayed. These two entries are what stop it, at the moment of
  # application rather than after the fact.
  #
  # So the two levers divide cleanly, and neither substitutes for the other:
  #
  #     forget   — retires a patch already in the ledger, permanently
  #     exclude  — stops the next equivalent commit from ever being applied
  #
  # The pre-rename spellings (hedra-sdk/Cargo.toml, hedra-types/Cargo.toml) are
  # gone from this list because no future commit can touch paths that no longer
  # exist, AND ed9886b4 is out of the ledger. Both conditions were required:
  # dropping the entries while that patch was still tracked would not have
  # retired it, it would have UN-SKIPPED it — and an un-skipped patch whose
  # files are gone is not inert. Its `theirs_snapshot` survives in the lock, so
  # base and ours both read null, the planner returns kind "new-file-only-user"
  # and the applicator writes the snapshot back to disk. That is how
  # hedra-types/.gitignore returned to main: fc264e8 added it (#60), the #72
  # regeneration f4a1372 deleted it, and 2f7fad9 "[fern-replay] Applied
  # customizations" restored it from patch-fc264e80's snapshot. That patch is
  # forgotten too now, so the next regeneration's deletion of that husk sticks.
  #
  # Do NOT reach for `moves:` (the other key LockManager reads out of this
  # file) to repoint an old path at a renamed crate. That resolves the patch
  # onto the live manifests and applies the stale stamp to them — the failure
  # this entry exists to prevent, with extra steps.
  #
  # Verified against fern-api 5.100.0 — `latest`, which setup-fern-cli installs
  # unpinned, and the version that ran the forget — in
  # ReplayApplicator.isExcluded / resolveFilePath / applyPatchWithFallback, the
  # merge planner they feed, and PatchDetector's forgotten-hash check.
  - hedra-cli-sdk/Cargo.toml
  - hedra-cli-types/Cargo.toml

  # The ENG-10226 identity-header merge in CliExecutor::new (ENG-10241, and the
  # 2026-08-13 regeneration that forced the issue).
  #
  # src/sdk_executor.rs is generated, and the one deviation it carries from
  # generated output is machine-owned: .github/scripts/patch-sdk-headers.py
  # re-derives the merge and re-inserts it after every regeneration, naming the
  # SDK crate from the config.rs path it is handed. Replay replaying a *recorded*
  # copy of that same fixup is therefore never useful — and it is actively
  # harmful, because the recorded copy is frozen while the tree is not:
  #
  #     patch-d06b3c36 ("fix: re-supply the X-Fern-* identity headers the CLI
  #     executor drops (#57)") records the pre-ENG-10291 body, which calls
  #     `hedra_sdk::ClientConfig::default()`. The crate is `hedra_cli_sdk` now.
  #     Replay puts the old body back over the freshly generated file; the result
  #     is neither the generated nor the patched shape, so patch-sdk-headers.py
  #     refuses to guess and refresh-lockfile.yml aborts BEFORE the version
  #     alignment — which is why `rust` then also reports the member crates at
  #     0.1.0/0.0.0. Both #85 (release/2.0.0) and #87 (main) hit this and both
  #     needed the same hand fix pushed onto the bot branch.
  #
  # This cannot age out on its own. A regeneration rebases a patch's
  # `base_generation` and drops snapshots for files that no longer exist, but
  # `patch_content` is frozen at detection — verified by diffing .fern/replay.lock
  # across the #87 regeneration, where d06b3c36's body is byte-for-byte the #57
  # diff from 2026-08-10.
  #
  # One entry retires the whole patch: isExcluded() is `files.some(...)` and
  # d06b3c36's file list is [hedra-sdk/src/config.rs, src/sdk_executor.rs].
  # Nothing is lost with it. That was the objection when this was first raised
  # (the config.rs half carried a real X-Fern-SDK-Version fix), and the ENG-10291
  # rename settled it: the generator no longer emits hedra-sdk/src/config.rs, so
  # the #72 regeneration dropped that hunk from `patch_content` and the #87 one
  # dropped its snapshot. What is left in the patch is only the stale executor
  # body. The X-Fern-SDK-Version literal is still repaired every regeneration by
  # patch-sdk-headers.py's first patch, which is where it belongs.
  #
  # The entry also covers what comes next. `fix(sdk): name the renamed SDK crate
  # in the executor identity merge`, the hand fix on #87's branch, reaches main
  # under its own SHA (merge-commit landing, see the detection note above) and is
  # itself detected as a customization on the next regeneration — a one-line diff
  # against a line that, with d06b3c36 skipped, no longer exists. Its file list is
  # [src/sdk_executor.rs], so this entry skips it too.
  - src/sdk_executor.rs

  # The ENG-10310 identity-header seeding in HttpConfig::build_client.
  #
  # Same shape as the entry above, and listed for the same reason rather than by
  # analogy: src/http.rs is generated, and its one deviation from generated
  # output is machine-owned — patch-sdk-headers.py re-derives the seeding after
  # every regeneration, naming the SDK crate from the config.rs path it is
  # handed. A *recorded* copy of that same fixup is therefore never useful, and
  # is actively harmful for the reason d06b3c36 demonstrated: patch_content is
  # frozen at detection, so the day the crate is renamed again Replay lays the
  # old crate name back over freshly generated code, the result matches neither
  # the generated nor the patched shape, and patch-sdk-headers.py refuses to
  # guess — aborting refresh-lockfile.yml before the version alignment.
  #
  # Excluded from the first commit rather than after the first failure: the
  # commit that adds this file's patch touches src/http.rs, so it is detected as
  # a customization on the very next regeneration. Without this entry that is a
  # ~20-line replayed hunk against generator-owned code, i.e. the identical trap,
  # and the whole point of ENG-10310 is to stop re-applying these by hand.
  #
  # That introducing commit is deliberately src/http.rs ALONE, per the
  # WHOLE-PATCH SEMANTICS note at the top of this file: the rest of ENG-10310
  # (the wire-contract lock, patch-sdk-headers.py, the workflow call sites) is a
  # separate commit, so this entry retires a patch whose file list is exactly
  # [src/http.rs] and drags nothing else into the skip. Everything in that second
  # commit is .fernignore'd anyway, so neither commit depends on Replay to
  # survive.
  - src/http.rs

  # NOT excluded, deliberately: hedra-cli-sdk/Cargo.toml,
  # hedra-cli-types/Cargo.toml and hedra-cli-sdk/src/config.rs — the post-rename
  # paths of the two stamps above. release/1.0.x and release/2.0.0 do list the
  # two manifests, because on a release branch a replayed `-dev` stamp is the
  # suffix the release exists to strip; that comment already records main as
  # having the gap on purpose, and this is not the change that closes it. On main
  # every regeneration re-derives all three stamps anyway (set-version.py and
  # patch-sdk-headers.py, both in refresh-lockfile.yml), so a replayed stale
  # `X.Y.Z-dev` is overwritten in the same run. The residual cost is the second
  # failure mode described above — the day the generator emits a different
  # default, those patches go unresolved and the regeneration PR shows "Action
  # required" — not a broken tree. Note that the refresh commit is one patch
  # across all three files, so an entry for any one of them would skip it whole.
  #
  # NOT excluded, deliberately: docs/superpowers/** (patch-d871322c, ~1.8k lines
  # of markdown in the ledger). Excluding it would be actively unsafe. Those
  # files are hand-written and the generator emits nothing under docs/, so
  # Replay re-adding them was the only thing keeping them in the tree; an
  # exclude here drops that without replacing it, which is precisely the #27 →
  # #29 sequence that deleted .gitattributes. .fernignore is the lever, and the
  # docs/ entry there is now in place — but it has NOT retired the patch, as
  # this note used to claim. The trailing slash defeats Replay's cleanse matcher
  # at 5.94.0; the docs/ entry in .fernignore carries the measurement.
