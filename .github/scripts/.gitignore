# Running the test suite imports set-version.py, which byte-compiles it.
# Kept here rather than in the root .gitignore: that file is generator-owned
# (it is not in .fernignore), so an entry there would be lost on the next
# regeneration. Everything under .github/ is .fernignore'd.
__pycache__/
